{
  "name": "mlx-ts",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "dependencies": {
    "@mlxts/core": "workspace:*",
    "@mlxts/nn": "workspace:*",
    "@mlxts/optimizers": "workspace:*"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "build": "bun run build:native",
    "build:native": "bun run scripts/build-native.ts"
  }
}
