# `@mlxts/lora`

Low-rank adapter transforms and adapter I/O for mlxts modules.

`@mlxts/lora` owns generic module traversal, LoRA application/removal, and
portable adapter save/load helpers on top of `@mlxts/nn`.
