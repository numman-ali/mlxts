# @mlxts/protocols

Zero-dependency protocol helpers shared by mlxts serving and agent surfaces.

This package is intentionally narrow. It currently owns model reasoning-tag
normalization so protocol adapters and agent clients do not drift when model
families emit native thinking markers.
