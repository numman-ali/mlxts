# `@mlxts/hub`

HuggingFace-compatible artifact loading for mlxts.

`@mlxts/hub` resolves local model folders and Hub snapshots, parses model
artifacts, and streams safetensors shard data for downstream model loaders.
