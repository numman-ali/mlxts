# Repo Memory

This file captures durable cross-session learnings for `mlxts` so future agent sessions do not need to rediscover the same sharp edges.

## How To Use This File

### Tier 1 — Must Read

- Read this section at the start of every session, immediately after `AGENTS.md`.
- Keep it short and durable. Promote only facts that should shape most future sessions.
- If Tier 1 grows, demote older or narrower items into Tier 2.

### Tier 2 — Lookup Log

- Do not read this section end to end by default.
- Search it when working on a related area.
- Append durable learnings here at the end of a session.

## Memory Update Rule

- Add durable learnings to Tier 2 using this template:
  - `(YYYY-MM-DD) [TAG] <learning> — refs: <file/doc/command>`
- Promote only cross-session critical items to Tier 1.
- Keep Tier 1 focused on durable repo facts and recurring sharp edges, not doctrine already stated in `AGENTS.md`.
- Archive obsolete items instead of silently deleting them.
- Do not put transient task notes or diary-style updates here.

## Tier 1 — Must Read

- `examples/nanogpt/` is the committed in-repo example and regression surface for the GPT proof path; prefer improving canonical `@mlxts/*` packages over deepening example-only abstractions, and document its commands from the example directory rather than as root scripts.
- When Nomi has authorized sub-agents, use them for non-trivial repo work. Complex changes in this repo benefit from a second opinion and deliberate orchestration across explorers/workers, especially when local context is already crowded.
- Heavy MLX commands are exclusive on one machine. Do not run benchmarks, soak runs, acceptance runs, or long training/proof commands in parallel.
- Runtime-sensitive production changes require a review artifact under `docs/reviews/`, plus `bun run check:runtime-review` before handoff.
- `bun run typecheck` and `bun run check:coverage` are required quality gates, not optional cleanup.
- Before writing a JS fallback for a GPU-facing operation, check whether `mlx-c` already exposes the op in `packages/core/native/build/_deps/mlx-c-src/mlx/c/ops.h`.
- For low-level backend questions, consult `.reference/mlx` and `.reference/mlx-c` alongside the vendored headers before inventing JS workarounds or custom native helpers.
- Keep runtime strategy out of public model identity and semantic names. Do not widen `CausalLM` or create duplicate model configs just to represent cache/backend/runtime variants.
- Before treating a performance gap as real, verify benchmark truth first. Paired reference runs must match decode shape and stopping behavior, and long-context or boundary-sensitive confirmation matters more than one flattering short run.

## Tier 2 — Lookup Log

- (2026-04-22) [PRODUCT] `examples/nanogpt/` is the committed nanoGPT example/regression surface; permanent contracts still belong in reusable package-owned surfaces instead of the example. — refs: `AGENTS.md`, `README.md`, `PLAN.md`
- (2026-04-22) [AGENTS] When Nomi authorizes sub-agents, treat them as standard force multipliers for non-trivial repo changes. Use them for second-opinion architecture checks, scoped implementation slices, and upstream truth-finding when local context is saturated; orchestration quality matters as much as using them at all. — refs: `AGENTS.md`, this session's Qwen3.5 / multimodal research thread
- (2026-04-22) [OPS] Heavy MLX commands are exclusive on a shared machine; do not run benchmark, soak, acceptance, memory, or long-run training commands in parallel. — refs: `AGENTS.md`
- (2026-04-22) [QUALITY] Runtime-sensitive diffs need a paired review artifact under `docs/reviews/`, and the artifact's `Files Reviewed` section must name the exact changed runtime-sensitive files. — refs: `AGENTS.md`, `docs/runtime-safety.md`
- (2026-04-22) [ARCH] Model contracts describe behavior, not runtime strategy; MoE stays a block-level swap inside `CausalLM`, and runtime/backend differences should not fork model identity. — refs: `AGENTS.md`, `docs/design-reasoning.md`
- (2026-04-22) [MLX-C] For GPU-facing functionality, check `mlx-c` first before introducing JS workarounds; fallback JS should stay limited to genuinely host-side work. — refs: `AGENTS.md`, `packages/core/native/build/_deps/mlx-c-src/mlx/c/ops.h`
- (2026-04-22) [REFERENCES] Keep first-party `.reference/mlx` and `.reference/mlx-c` available and up to date for low-level capability audits. Use them to decide whether to bind an existing MLX/MLX-C primitive, rely on compile, or introduce a custom native seam. — refs: `AGENTS.md`, `.reference/mlx`, `.reference/mlx-c`
- (2026-04-22) [MLX] First-party MLX already covers most low-level primitives we need before writing custom C helpers: convs, scatter/masked-scatter, slice updates, fast norm/rope/SDPA, compile, and custom kernel surfaces. The real gaps are narrower: hybrid cache policy, Qwen3.5-style recurrent linear attention, and selective `mlx-c` binding omissions such as the `slice_update_{add,prod,max,min}` family that exists in C++ but is not exposed in `mlx-c`. — refs: `.reference/mlx/mlx/ops.h`, `.reference/mlx-c/mlx/c/ops.h`, `.reference/mlx-c/mlx/c/fast.h`, `.reference/mlx-c/mlx/c/compile.h`
- (2026-04-22) [OPS] The canonical Phase 8 training proof currently lives as a manual self-hosted Apple Silicon workflow plus local `bun run examples/train-proof/index.ts`; do not describe it as an every-push CI gate until we intentionally promote it. — refs: `.github/workflows/training-proof.yml`, `examples/train-proof/README.md`, `docs/gates-and-milestones.md`
- (2026-04-22) [BENCHMARKS] Gemma 4 parity work showed that benchmark fairness can dominate the apparent gap: confirm EOS handling, decode length, and reference harness behavior before attributing a delta to model/runtime code. Paired truth comes before optimization heroics. — refs: `packages/transformers/scripts/benchmark-mlx-lm.py`, `packages/transformers/scripts/benchmark-common.ts`, `docs/reviews/2026-04-20-gemma4-parity-experiments.md`
- (2026-04-22) [FINE-TUNING] Narrow-task data teaches a model more effectively than broad assistant-style chat data. Small mixed SFT sets are fine for stack proof, but held-out behavior, not lower loss alone, decides whether a repo-specific LoRA actually got better. — refs: `examples/chat-canary/README.md`, `examples/chat-canary/review-judge/README.md`, `docs/reviews/2026-04-22-review-judge-lora-proof.md`
- (2026-04-22) [FINE-TUNING] Experimental proof assets should stay explicitly labeled as experimental. Keep promising LoRA datasets and canaries in the repo when they teach something real, but do not silently let them become a claimed product surface or finished benchmark. — refs: `examples/chat-canary/README.md`, `examples/chat-canary/review-judge/README.md`, `docs/reviews/2026-04-22-review-judge-lora-proof.md`
- (2026-04-22) [ALIGN] DPO proof reporting must use reference-aware reward metrics, not just policy-only chosen-vs-rejected log-prob wins. `raw_pref_acc` is a supplemental debug signal; the canonical readout is reward accuracy plus reward margin, with chosen/rejected rewards and log-probs recorded alongside loss. — refs: `packages/align/src/loss-utils.ts`, `examples/train-proof/runtime.ts`, `examples/train-proof/stages.ts`, `examples/train-proof/README.md`
- (2026-04-22) [ALIGN] When Phase 8 examples start duplicating dataset-level eval and fixed-step SFT/DPO loop glue, extract that recipe layer into `@mlxts/align` rather than growing a new framework package or deepening `examples/nanogpt/`. Keep CLI policy, proof presets, and teaching-specific flows in examples. — refs: `packages/align/src/recipes.ts`, `examples/lora-finetune/runtime.ts`, `examples/train-proof/runtime.ts`, `PLAN.md`
- (2026-04-22) [DOCS] Example-owned commands should be documented from the example directory (`cd examples/nanogpt && bun run ...`), not as root `package.json` scripts. — refs: `README.md`, `docs/setup.md`, `docs/product-surfaces.md`
- (2026-04-22) [ALIGN] Raw chat normalization belongs in `@mlxts/align` once multiple Phase 8 examples are reimplementing “build example, skip malformed rows, skip overlong rows, report kept/skipped counts.” Keep dataset-specific parsing and proof/example policy outside the package, but keep the normalization loop package-owned. — refs: `packages/align/src/chat-preparation.ts`, `examples/lora-finetune/data.ts`, `examples/train-proof/prepare.ts`
- (2026-04-22) [MULTIMODAL] Keep file and image decoding outside `@mlxts/transformers`; the package should own checkpoint-side preprocessing truth (`preprocessor_config.json`, prompt preparation, patchification), while examples or apps own local file I/O and resize/decode plumbing. — refs: `examples/qwen3_5-image/`, `packages/transformers/src/families/qwen3_5/preprocessing.ts`
- (2026-04-22) [MULTIMODAL] Qwen 3.5 / Qwen 3.6 wrapper prompts need batch-aware rope-delta reuse and prompt-side image placeholder expansion. Keep the repeated image-token span and per-batch position recovery inside the package-owned helper instead of scattering that logic across examples. — refs: `packages/transformers/src/families/qwen3_5/conditional.ts`, this session's Qwen multimodal tranche
- (2026-04-22) [OPS] Wrapper commands like `examples/nanogpt/src/run/acceptance.ts` and `examples/nanogpt/src/run/soak.ts` must not hold the shared runtime lock while launching the supervised run. The spawned supervisor owns the heavy MLX lock; wrapper-level post-run MLX work can reacquire it only after the supervised run exits. — refs: `examples/nanogpt/src/run/acceptance.ts`, `examples/nanogpt/src/run/soak.ts`, `examples/nanogpt/src/run/acceptance.test.ts`, `docs/reviews/2026-04-22-examples-nanogpt-migration.md`
- (2026-04-22) [GEMMA4] Dense-only fast paths must stay dense-only. If a family implementation reads raw `.weight` tensors for a compiled path, wrapped or quantized modules need a semantic fallback rather than being forced through the dense shortcut. — refs: `packages/transformers/src/families/gemma4/mlp.ts`, `packages/transformers/src/families/gemma4/mlp.test.ts`, `docs/reviews/2026-04-22-gemma4-mlp-lora-compatibility.md`
- (2026-04-22) [QWEN] Qwen 3.5 / Qwen 3.6 full-attention `q_proj` outputs are packed per head as `[query, gate]`, not as one global query half followed by one global gate half. When splitting packed projections, reshape to `[batch, seq, heads, 2 * headDim]` first, then split the last axis per head before flattening back. — refs: `packages/transformers/src/families/qwen3_5/attention.ts`, `packages/transformers/src/families/qwen3_5/attention.test.ts`, `.reference/mlx-lm/mlx_lm/models/qwen3_next.py`
- (2026-04-22) [TOKENIZERS] Hugging Face `tokenizer.json` added tokens can use sparse IDs above the base vocab size. `BPETokenizer` decode tables must register `added_tokens` by ID, not just base vocab entries, or real checkpoints like Qwen 3.6 will fail to decode tokens such as `<think>` / `</think>` even when generation is correct. — refs: `packages/tokenizers/src/bpe-base.ts`, `packages/tokenizers/src/bpe.test.ts`, `mlx-community/Qwen3.6-27B-4bit`
- (2026-04-23) [GENERATION] Long text streaming must not re-decode the full generated prefix on every token. That exact-but-host-heavy pattern turns large `maxTokens` runs into repeated O(n) decode work, severe slowdown, and avoidable crash pressure in chat surfaces. Batch stream flushes and do one final exact decode instead. — refs: `packages/transformers/src/generation.ts`, `packages/transformers/src/generation.test.ts`, `docs/reviews/2026-04-23-long-generation-chat-streaming.md`
- (2026-04-23) [GENERATION] `mlx-lm` uses one-token async lookahead during generation, so async lookahead is not a smell by itself. Keep cache views stable as values handed to attention, and treat huge long-context runs as a cache-policy problem requiring max-KV, quantized KV, paged cache, or memory-budget controls. — refs: `.reference/mlx-lm/mlx_lm/generate.py`, `.reference/mlx-lm/mlx_lm/models/cache.py`, `.reference/transformers/src/transformers/cache_utils.py`, `.reference/omlx/omlx/memory_monitor.py`
- (2026-04-23) [SERVE] `@mlxts/serve` should stay protocol-adapter-thin: OpenAI completions, chat completions, Responses/OpenResponses, and Anthropic messages normalize directly into one internal generation request, not into each other. Engines own generation; adapters own wire shape. — refs: `packages/serve/src/types.ts`, `packages/serve/src/protocols/openai-completions.ts`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [SERVE] Partially superseded by the 2026-04-24 full-KV scheduler tranche: admission micro-batching and continuous token-level batching are still different layers. `createMicroBatchingGenerationEngine()` is a correctness/test seam for coalescing nearby non-streaming requests into `generateBatch()`; broader production throughput still needs scheduler/cache-aware engines behind the same contract. — refs: `packages/serve/src/batching-engine.ts`, `examples/serve-completions/index.ts`, `.reference/vllm-mlx/docs/guides/continuous-batching.md`, `.reference/omlx/omlx/engine_core.py`
- (2026-04-23) [SERVE] Partially superseded by later streaming, cancellation, and Responses tranches: real model serving should reject unsafe generation lengths at admission, expose `/v1/models`, and keep auth/model-id routing separate from the transformer engine. The original first model-backed surface was intentionally non-streaming, but streaming is now canonical where implemented. — refs: `packages/serve/src/request-limits.ts`, `packages/serve/src/protocols/openai-models.ts`, `packages/serve/src/model-server.ts`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [SERVE] Real model serving is a first-class package API and CLI responsibility, not an `examples/` product surface. Examples can demonstrate flows, but operator-facing load/serve behavior belongs in `@mlxts/serve` and should be validated through package tests, CLI help, package builds, and root build. — refs: `packages/serve/src/model-server.ts`, `packages/serve/src/cli.ts`, `packages/serve/package.json`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [AGENT] Agent loops belong in `@mlxts/agent`; serving owns wire protocols and model execution, transformers own chat-template/tool prompt compilation, and agent owns model→tool→observation→continue orchestration. Avoid homemade completions prompt renderers when a package-owned chat endpoint and interaction profile exist. — refs: `packages/agent/src/loop.ts`, `packages/agent/src/chat-model.ts`, `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/transformers/src/chat-template.ts`
- (2026-04-23) [AGENT] Superseded: an earlier stabilizer made agent CLI turns default to deterministic generation after a sampled Qwen3.6 smoke hit native crash behavior. The product-standard fix is model-native defaults by omission, explicit `--greedy` / `--deterministic` for tool-loop safety, and targeted sampling containment/coverage instead of hiding model defaults. — refs: `packages/agent/src/cli.ts`, `packages/agent/src/chat-model.ts`, `packages/transformers/src/infrastructure/sampling/runtime.ts`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [SERVE] OpenAI-compatible adapters should preserve omitted sampling fields (`temperature`, `top_p`, `top_k`) so checkpoint `generation_config.json` applies. Qwen-style thinking controls belong in chat-template kwargs, and generated `<think>` text should surface as `reasoning_content`/`reasoningContent` rather than leaking into visible assistant content. — refs: `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/serve/src/protocols/openai-completions.ts`, `packages/agent/src/chat-model.ts`, `packages/transformers/src/chat-template.ts`
- (2026-04-23) [AGENT] Qwen3.6 may emit native tool calls as `<tool_call><function=list_files>...</function></tool_call>` even when the agent provides OpenAI tool schemas and a JSON envelope instruction. The agent parser must accept both the package JSON envelope and Qwen-style function blocks for local interactive use. — refs: `packages/agent/src/tool-calls.ts`, `packages/agent/src/tool-calls.test.ts`, live `qwen-smoke` PTY run
- (2026-04-23) [AGENT] Real interactive agent validation should cover multiple REPL turns, reasoning display, and at least one real tool observation continuing into a final answer. A silent max-iteration return is a usability bug; surface it as an explicit `[agent]` stop notice. — refs: `packages/agent/src/cli.ts`, `packages/agent/src/cli.test.ts`, live `qwen-smoke` PTY run
- (2026-04-23) [AGENT] `@mlxts/agent` can consume OpenAI chat-completion SSE through the optional `AgentModel.stream()` contract. The loop aggregates streamed content, reasoning, and `delta.tool_calls` before tool execution while emitting incremental `model_delta` events for CLI responsiveness; keep non-streaming `complete()` models as a compatible fallback. — refs: `packages/agent/src/types.ts`, `packages/agent/src/chat-model.ts`, `packages/agent/src/loop.ts`, `packages/agent/src/cli.ts`
- (2026-04-23) [EXAMPLES] Example-only runnable entrypoints do not all need declaration output. For `examples/nanogpt`, keep JS bundling for CLI/manager/acceptance/soak/supervisor/memory, but declaration build should cover exported package surfaces so repo-level helper imports like `scripts/runtime-command-lock.ts` do not leak into the example package type boundary. — refs: `examples/nanogpt/tsconfig.build.json`, `scripts/runtime-command-lock.ts`
- (2026-04-23) [QWEN] Qwen 3.6 decode memory failures came from multiple Qwen-only lifetime issues, not the server or async lookahead. The catastrophic leak was `gatedDeltaSequence()` retaining the final recurrent state without freeing its local handle (`~153 MB/token`). The remaining `~4 MB/token` slope was also wrong and came from hidden owned intermediates in the Qwen decoder block and gated-delta conv/projection path. The prompt 128 / generation 128 benchmark now measures flat active memory, while Llama, Gemma 3, and Gemma 4 controls stay flat. — refs: `packages/transformers/src/families/qwen3_5/block.ts`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `packages/transformers/src/families/qwen3_5/gated-delta.test.ts`, `scripts/runtime-sensitive-ops.ts`, `packages/transformers/scripts/benchmark-generation.ts`, `docs/reviews/2026-04-23-qwen3_6-decode-memory.md`
- (2026-04-23) [SERVE] OpenAI `stop` semantics must stay aligned across completions and chat, and SSE writers must flush buffered stop/reasoning tails even if a stream ends before an explicit final `done` event. Shared admission gates also need to release permits when `stream()` fails before yielding, or single-model serving can deadlock under `maxConcurrentRequests=1`. — refs: `packages/serve/src/protocols/openai-stop.ts`, `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/serve/src/server-streaming.ts`, `packages/serve/src/concurrency-engine.ts`, `docs/reviews/2026-04-23-serve-chat-streaming-and-admission.md`
- (2026-04-23) [SERVE] Bun SSE serving needs lifecycle-aware handling: disable Bun's per-request idle timeout for streaming responses, emit `request_complete` only after the stream actually finishes, and surface client disconnects as explicit cancellation instead of silent early socket loss. — refs: `packages/serve/src/server.ts`, `packages/serve/src/server-streaming.ts`, `docs/reviews/2026-04-23-serve-streaming-lifecycle.md`, `https://bun.sh/docs/runtime/http/server`
- (2026-04-23) [SERVE] Partially superseded by static batch generation and the first full-KV continuous batching tranche: admission micro-batching is still not a substitute for scheduler-owned decode. Qwen hybrid caches, Gemma layer-pattern caches, sampling, streaming, prefix cache, and paged cache remain separate cache-semantics work. — refs: `packages/transformers/src/types.ts`, `packages/transformers/src/infrastructure/cache/index.ts`, `packages/serve/src/batching-engine.ts`, `.reference/mlx-lm/mlx_lm/models/cache.py`, `docs/reviews/2026-04-23-serve-streaming-lifecycle.md`
- (2026-04-23) [GENERATION] The first batch generation foundation is static, full-cache, non-streaming, and greedy-only: `BatchKVCache` tracks per-row offsets/left padding, LLaMA-like full-cache models can consume it, and `generateBatchTokens()` filters completed rows. Do not call this continuous batching; Qwen hybrid caches, Gemma layer-pattern/sliding caches, sampled batch decode, and serving scheduler integration remain separate tranches. — refs: `packages/transformers/src/infrastructure/cache/batch.ts`, `packages/transformers/src/infrastructure/generation/batch.ts`, `packages/transformers/src/families/llama-like/model.test.ts`, `docs/reviews/2026-04-23-static-batch-generation.md`
- (2026-04-23) [SERVE] Partially superseded by the 2026-04-24 full-KV continuous scheduler: the transformer-backed serve engine can use `generateBatchTokens()` for eligible non-streaming greedy full-cache LLaMA-like groups, and eligible LLaMA-like requests now also have a scheduler path. Keep Qwen, Gemma 3/4 layer-pattern caches, sampled/model-native-default requests, and streaming on shape-specific paths until scheduler support handles those decode/cache shapes. — refs: `packages/serve/src/transformers-engine-generation.ts`, `packages/serve/src/model-server.test.ts`, `docs/reviews/2026-04-23-serve-static-batch-integration.md`
- (2026-04-23) [SERVE] `serveLoadedModels()` is the first-class programmatic multi-model surface for already-loaded checkpoints. Each model id gets its own transformer engine, request limits, concurrency gate, and micro-batch queue before the shared router dispatches requests, so multiple local models do not accidentally share cache/runtime state. — refs: `packages/serve/src/model-server.ts`, `packages/serve/README.md`, `docs/reviews/2026-04-23-serve-loaded-models.md`
- (2026-04-23) [GENERATION] Static greedy batch generation now accepts either scalar `maxTokens` or per-row `maxTokens` arrays, emits row-indexed token/done events, and skips zero-token rows before prefill. Serving uses that to batch eligible mixed-`max_tokens` requests while preserving original result order. — refs: `packages/transformers/src/infrastructure/generation/batch.ts`, `packages/serve/src/transformers-engine-generation.ts`, `docs/reviews/2026-04-23-static-batch-decode-v2.md`
- (2026-04-23) [SERVE] `serveModels()` is the first-class programmatic multi-source loader for serving several local or Hub checkpoints behind one endpoint. It loads sequentially to avoid RAM spikes, annotates progress with source/model context, owns the loaded models, and cleans up already-loaded models if a later load or server start fails. — refs: `packages/serve/src/model-sources.ts`, `packages/serve/src/model-sources.test.ts`, `docs/reviews/2026-04-23-serve-model-sources.md`
- (2026-04-23) [SERVE] The `mlxts-serve` CLI supports repeatable `--model <source>` / `--model <model-id=source>` entries for multi-model endpoints and routes them through `serveModels()`. Keep `--model-id` as a positional single-model shorthand only; reject ambiguous mixes instead of guessing. — refs: `packages/serve/src/cli-options.ts`, `packages/serve/src/cli.ts`, `docs/reviews/2026-04-23-serve-cli-multi-model.md`
- (2026-04-23) [SERVE] Partially superseded by the streaming tool-call tranche: non-streaming OpenAI chat completions format valid generated `<tool_call>...</tool_call>` envelopes as structured `message.tool_calls` with `finish_reason: "tool_calls"` when tools were enabled. The parser is local to `@mlxts/serve` and shared with streaming support. — refs: `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/serve/src/protocols/openai-chat-tool-calls.ts`, `docs/reviews/2026-04-23-serve-chat-tool-calls.md`
- (2026-04-23) [SERVE] Streaming OpenAI chat completions now buffer generated `<tool_call>...</tool_call>` envelopes and emit structured `delta.tool_calls` chunks with `finish_reason: "tool_calls"` when tools were enabled. Keep parsing conservative: malformed or incomplete tool-call-looking text remains visible content, and Qwen-style native function XML is parsed only after a complete envelope arrives. — refs: `packages/serve/src/protocols/openai-chat-tool-call-stream.ts`, `packages/serve/src/server-streaming.ts`, `docs/reviews/2026-04-23-serve-chat-tool-calls.md`
- (2026-04-23) [QWEN] Qwen 3.6 parity must use the fixed multi-trial harness before claiming speed deltas: capture `mlx-lm` before loading the Bun model, forward `prefill_step_size`, match warmup/trial count, and keep local-only profiling explicit with `--skip-mlx-lm-reference`. Compiling Qwen decay factors is a proven small win (`24.898 -> 25.945 tok/s` on prompt 128 / decode 128), but compiling the multi-output recurrent step through `compileMany()` crashed Bun and should not be retried casually; the next real seam is a narrow fused/native gated-delta helper with the TS recurrence as fallback/oracle. — refs: `packages/transformers/scripts/benchmark-generation-parity.ts`, `packages/transformers/scripts/benchmark-mlx-lm.py`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [QWEN] The Qwen 3.6 parity keeper stack is: native Metal gated-delta helper, mixed-dtype q/k/v/g/beta inputs with fp32 recurrent state, contiguous conv-cache tail, causal SDPA marker for non-window cached prefill, one hoisted full-attention mask per model forward, and eval-only fused quantized `b/a` projections. This moved staged rungs to practical parity: `1024/128` slightly ahead of `mlx-lm`, `10000/128` about `0.7%` under, `1024/1024` about `0.3%` under, and local long outputs `128/10000` plus `128/20000` completed at `0.07 MB/token` active slope. — refs: `packages/core/native/mlxts_core_ops.cpp`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `packages/transformers/src/families/qwen3_5/model.ts`, `packages/transformers/src/infrastructure/masks.ts`, `packages/nn/src/quantized-linear.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [BENCHMARKS] For Qwen 3.6 capability claims, use the existing suite as a staged matrix instead of inventing a new harness: paired `bench:generation:parity` for `128/128`, `1k/128`, `5k/128`, `10k/128`, and output rungs like `1k/512` and `1k/1024`; local `bench:generation:parity --skip-mlx-lm-reference` for extreme outputs such as `10k` and `20k`; and `bench:generation:context` for retrieval/context ladders. Long-context config must read nested `text_config.max_position_embeddings`; Qwen 3.6 advertises `262144`, but local `128k` already peaked at `42.550 GB`, so 262k should wait for serving admission/memory preflight. — refs: `packages/transformers/scripts/benchmark-generation-parity.ts`, `packages/transformers/scripts/benchmark-long-context.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [LONG-CONTEXT] Qwen long-context retrieval benchmarks should disable thinking at prompt compilation and grade the first non-empty generated answer line for fixed-length decode, while still printing the full response. Qwen3.6 retrieved markers exactly at `32k`, `64k`, and `128k` with `0.00 MB/token` active decode slope; measured peaks were `25.995 GB`, `31.410 GB`, and `42.550 GB` respectively. — refs: `packages/transformers/scripts/benchmark-long-context.ts`, `packages/transformers/scripts/benchmark-long-context.test.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [SERVE] Partially superseded by later admission, cancellation, long-context streaming, and benchmark tranches: after Qwen's long-context/long-output proof, the next reliability gap was serving orchestration rather than another decode micro-op. Qwen-aware scheduler/cache support remains future work before claiming high-concurrency local serving. `mlx-lm` has batch generator patterns, `vllm-mlx` has scheduler/prefix-cache structure, `omlx` has memory enforcement/pinning, and `rapid-mlx` has chunked prefill/decode interleaving and streaming post-processing worth studying. — refs: `packages/serve/src/batching-engine.ts`, `packages/serve/src/transformers-engine-generation.ts`, `.reference/mlx-lm/mlx_lm/generate.py`, `.reference/vllm-mlx/vllm_mlx/scheduler.py`, `.reference/omlx/omlx/process_memory_enforcer.py`, `.reference/rapid-mlx/vllm_mlx/scheduler.py`
- (2026-04-24) [SERVE] Long-context serving admission should name separate budgets for generated output, prompt/prefill size, and total context. `--max-prompt-tokens` rejects oversized prompts before cached prefill starts; `/info` reports configured admission metadata and checkpoint-declared context windows, but this is not a memory guarantee for huge Qwen contexts. — refs: `packages/serve/src/model-context.ts`, `packages/serve/src/transformers-engine-shared.ts`, `packages/serve/src/server-info.ts`, `docs/reviews/2026-04-24-serve-prompt-admission.md`
- (2026-04-24) [SERVE] Memory-aware serving admission now uses `--gpu-memory-utilization` / `gpuMemoryUtilization` as a best-effort guardrail before prefill starts. The estimator must account for family cache geometry, including Qwen hybrid recurrent/conv state and Gemma sliding/full/global-head KV layers; when config shape is unknown, skip rather than invent certainty. This is admission preflight, not a continuous-batching scheduler guarantee. — refs: `packages/serve/src/model-context.ts`, `packages/serve/src/transformers-engine-shared.ts`, `packages/serve/AGENTS.md`, `docs/reviews/2026-04-24-serve-memory-preflight.md`
- (2026-04-24) [SERVE] Serving cancellation must be a generation contract, not only an SSE writer detail. HTTP aborts, stream disconnects, and server shutdown now attach `abortSignal` to normalized requests; transformer generation checks it between prefill chunks and decode steps, and queued concurrency/micro-batch work rejects before dispatch. This remains cooperative cancellation, not scheduler preemption or true pull-driven SSE backpressure. — refs: `packages/serve/src/server.ts`, `packages/serve/src/server-abort.ts`, `packages/serve/src/concurrency-engine.ts`, `packages/serve/src/batching-engine.ts`, `packages/transformers/src/infrastructure/generation/cancellation.ts`, `packages/transformers/src/infrastructure/generation/runtime.ts`, `docs/reviews/2026-04-24-serve-cancellation.md`
- (2026-04-24) [SERVE] Endpoint-level serving benchmarks should use the package-owned `bun run bench:serve` harness, not raw model decode numbers, when claiming serving quality. It defaults to cached/local-only checkpoints, drives exact token-array prompts through `/v1/completions`, preserves model-native sampling unless `--greedy` is explicit, and reports admission micro-batch events separately from real static batch events so Qwen/Gemma single-request fallbacks are not overstated as batching. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/benchmark-serve-options.ts`, `packages/serve/README.md`
- (2026-04-24) [SERVE] Serving benchmark ladders need a streaming mode, not just buffered completion timing. `bun run bench:serve --stream` drives `/v1/completions` SSE with `stream_options.include_usage=true` and reports mean TTFT, stream chunk count, streamed bytes, and usage-derived token counts; tiny output rungs can collapse into one text chunk because the transformer engine batches stream decoding, so backpressure/cadence claims need larger generation-token rungs. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/README.md`
- (2026-04-24) [SERVE] Exact-length endpoint throughput ladders need an explicit EOS override, not a hidden serving default. Use `bun run bench:serve --ignore-eos` when comparing against in-process parity runs that intentionally decode the full requested token count; normal OpenAI-compatible serving still honors EOS unless the nonstandard `ignore_eos` extension is present. — refs: `packages/serve/src/protocols/openai-completions.ts`, `packages/serve/src/transformers-engine-shared.ts`, `packages/serve/scripts/benchmark-serve-options.ts`, `docs/reviews/2026-04-24-serve-ignore-eos-benchmark-control.md`
- (2026-04-24) [SERVE] `/v1/responses` text support should be useful but honest: accept string input and text-only message item arrays, stream semantic Responses SSE events for text/reasoning, preserve model-native sampling defaults, and keep tools, persistence, multimodal input, prompt templates, and structured output explicitly rejected until implemented for real. — refs: `packages/serve/src/protocols/openai-responses.ts`, `packages/serve/src/protocols/openai-responses-input.ts`, `packages/serve/src/server-responses-streaming.ts`, `docs/reviews/2026-04-24-serve-responses-text-streaming.md`
- (2026-04-24) [SERVE] Do not call admission coalescing continuous batching. Reference serving stacks define continuous batching as a scheduler-owned active decode loop that can insert/remove rows between token steps, with collectors and cancellation per request. The clean `mlxts` tranche is full-KV greedy scheduling first; Qwen hybrid caches, Gemma sliding/global caches, sampling, prefix cache, paged cache, SSD cache, and VLM batching should stay separate until their cache semantics are represented. — refs: `.reference/mlx-lm/mlx_lm/generate.py`, `.reference/vllm-mlx/vllm_mlx/scheduler.py`, `.reference/omlx/omlx/scheduler.py`, `packages/serve/src/batching-engine.ts`
- (2026-04-24) [SERVE] Bun SSE streams can enqueue chunks internally yet fail to deliver progressive bytes to clients if the writer only advances through microtasks during long decode. Keep HTTP flush cadence in the serving bridge by yielding a macrotask after processed stream events; do not move sleeps into model generation. Benchmark streaming TTFT to catch this class. — refs: `packages/serve/src/server-streaming.ts`, `packages/serve/src/server-responses-streaming.ts`, `packages/serve/scripts/benchmark-serve.ts`, `docs/reviews/2026-04-24-serve-stream-flush-cadence.md`
- (2026-04-24) [SERVE] Long buffered generation requests need Bun's request timeout disabled before generation starts, not after a response object exists. Streaming already returns quickly enough to set request lifetime on the response path; non-streaming long-context completions can spend minutes in `engine.generate()` first. — refs: `packages/serve/src/server.ts`, `packages/serve/src/server.test.ts`, `docs/reviews/2026-04-24-serve-long-buffered-timeouts.md`
- (2026-04-24) [SERVE] Huge-context streaming can still time out if prefill is a synchronous wall with no body bytes. The durable serving pattern is cooperative streaming prefill that yields between chunks plus SSE comment heartbeats while waiting for the model iterator. Qwen 3.6 `65536/128` streaming completed after this fix with `31.406 GB` peak and flat active memory; buffered JSON remains a poor acceptance shape for that rung. — refs: `packages/transformers/src/infrastructure/generation/runtime-streaming.ts`, `packages/transformers/src/infrastructure/generation/helpers.ts`, `packages/serve/src/server-sse-heartbeat.ts`, `docs/reviews/2026-04-24-serve-long-context-streaming.md`
- (2026-04-24) [SERVE] The first true continuous batching tranche is full-KV, greedy, non-streaming only. `ContinuousBatchTokenScheduler` owns waiting/active rows, `BatchKVCache.filter()`/`extend()`, per-row cancellation, and a bounded model execution lane; `serveLoadedModel()` routes eligible LLaMA-like requests through it and reports `continuous_admissions` separately from `static_batches` and admission micro-batches. Qwen hybrid, Gemma sliding/global, sampling, streaming, paged/prefix cache, and VLM batching remain separate cache-semantics work. — refs: `packages/transformers/src/infrastructure/generation/continuous-batch.ts`, `packages/serve/src/transformers-engine-continuous.ts`, `packages/serve/scripts/benchmark-serve.ts`, `docs/reviews/2026-04-24-serve-continuous-batching.md`
- (2026-04-24) [BENCHMARKS] Serving capability ladders should be explicit and evidence-preserving: use `bench:serve --rungs 128x128@1,... --report-json <path>` for staged endpoint runs, read p95/max request latency plus batch row counters before claiming concurrency, and use `bench:generation:parity --require-mlx-lm-reference` for publishable mlx-lm comparisons. Token-array completions benchmarks prove exact-token endpoint behavior, not chat/Responses/tool quality. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/benchmark-serve-options.ts`, `packages/transformers/scripts/benchmark-common.ts`, `docs/runtime-safety.md`
- (2026-04-24) [SERVE] Qwen 3.6 endpoint serving is capable through `131072/128` on the 64 GB local machine, but 128k is not yet ergonomic: streaming completed with `42.543 GB` peak, flat active memory, and `finish_reasons=length`, but TTFT was about `858.5s` and post-TTFT decode fell to `19.624 tok/s`. Treat the next Qwen serving gap as long-prefill scheduling/chunking plus peak-memory parity, not basic decode correctness. — refs: `docs/reviews/2026-04-24-qwen-serve-benchmark-ladder.md`, `.tmp/qwen36-serve-128k-stream.json`
- (2026-04-24) [SERVE] `bench:serve --protocol chat|responses` is the package-owned protocol-health path for chat completions and text Responses. Keep `--protocol completions` as the exact token-array throughput/parity mode; chat and Responses use deterministic text prompts and should not be compared as exact token-array runs. Streaming protocol benchmarks must fail if the stream ends without usage or a finish reason. — refs: `packages/serve/scripts/benchmark-serve-completions.ts`, `packages/serve/scripts/benchmark-serve-options.ts`, `docs/reviews/2026-04-24-serve-protocol-benchmarking.md`
- (2026-04-24) [LONG-CONTEXT] Broad context-window recall claims need early/middle/late marker placement, not only the default late-position needle. `bench:generation:context --needle-placements all --report-json <path>` records actual prompt tokens, marker token span/center, memory, timing, and partial JSON after each completed placement. — refs: `packages/transformers/scripts/benchmark-long-context.ts`, `packages/transformers/scripts/benchmark-long-context.test.ts`
- (2026-04-24) [SERVE] Full-KV continuous batching needs prefill fairness, not only row insertion at decode boundaries. Long waiting prompts should be chunk-prefilled between active decode steps and emit prefill progress before being merged into the active batch; Qwen/Gemma remain separate cache-semantics work. — refs: `packages/transformers/src/infrastructure/generation/continuous-batch.ts`, `packages/transformers/src/infrastructure/generation/continuous-batch-helpers.ts`, `packages/serve/src/transformers-engine-continuous.ts`
- (2026-04-24) [SERVE] Use `bench:serve --request-stagger-ms <n>` to test waiting-row scheduler fairness separately from simultaneous admission-window coalescing. Reports include the configured request stagger and per-rung arrival span, while per-request latency excludes the artificial launch delay. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/benchmark-serve-options.ts`, `packages/serve/README.md`
- (2026-04-24) [SERVE] Completions endpoint benchmarks should build deterministic token-array prompts only. Do not synthesize/tokenize text for completions rungs; Gemma 4 exposed that as a pre-benchmark stall before generation even started. Chat and Responses benchmarks still synthesize text prompts because they are protocol-health paths. — refs: `packages/serve/scripts/benchmark-serve-prompts.ts`, `packages/serve/scripts/benchmark-serve-prompts.test.ts`
- (2026-04-24) [SERVE] Qwen 3.6 27B 4bit serving can stream long outputs through the endpoint at `1024x10000@1` and `1024x20000@1` without crash or timeout. The 20k run held `26.597 tok/s`, `19.934 GB` peak, `1.547 GB` cache memory, and `0.013 GB` active delta, so remaining long-output work is quality/ergonomics rather than basic stability. — refs: `docs/reviews/2026-04-24-qwen-serve-benchmark-ladder.md`, `.tmp/qwen36-serve-output-20k-stream.json`
- (2026-04-24) [AGENT] Interactive streaming CLIs must render deltas through a continuous terminal writer, not section-format each fragment through `console.log`; otherwise token chunks split words across lines and make real streaming look broken. When validating a user command that runs `packages/*/dist/*.js`, rebuild the package dist before testing. — refs: `packages/agent/src/cli.ts`, `packages/agent/src/cli.test.ts`, `packages/agent/dist/cli.js`
- (2026-04-26) [QWEN] Qwen 3.6 27B 4bit load footprint parity depended on two separate fixes: keep MLX quantized embeddings packed with `QuantizedEmbedding`, and make generic top-level `qwen3_5` `loadCausalLM()` text-only while preserving full VLM loading through `loadQwen3_5ForConditionalGeneration()`. The real cached checkpoint now loads as `qwen3_5_text` at about `15.134 GB` active memory. — refs: `packages/nn/src/quantized-embedding.ts`, `packages/transformers/src/families/qwen3_5/config.ts`, `packages/transformers/src/families/qwen3_5/load.ts`, `docs/reviews/2026-04-26-qwen-gemma-regression-guardrails.md`
- (2026-04-26) [BENCHMARKS] Use `bun run --filter '@mlxts/transformers' regression:models` for cheap Qwen/Gemma-focused tests and `bun run packages/transformers/scripts/regression-model-matrix.ts --decode-smoke` before high-risk model commits when cached real Qwen/Gemma checkpoints are available. The real path holds the runtime lock, isolates memory accounting, and enforces strict decode smoke budgets rather than accepting benchmark warnings as success. — refs: `packages/transformers/scripts/regression-model-matrix.ts`, `docs/reviews/2026-04-26-qwen-gemma-regression-guardrails.md`
- (2026-04-26) [AGENTS] Sub-agents are not only for exploration. For complex repo work, the lead agent may delegate bounded implementation slices to workers when the context and write scope are clear, but the lead must review and integrate every code change before shipping. Use explorers for architecture truth and workers for disjoint edits; never treat delegated code as self-shipping. — refs: `AGENTS.md`, Nomi guidance on sub-agent orchestration
- (2026-04-26) [SERVE] Use `bun run regression:qwen-gemma -- --profile quick` as the normal focused Qwen/Gemma guardrail, `--profile real` when cached real checkpoints are available, and `--profile substantial` for heavier endpoint/context proof. Serving budgets must distinguish end-to-end completion TPS (includes prefill/TTFT) from post-TTFT completion TPS; Qwen 1024x128 streaming measured about `14 tok/s` end-to-end but about `32 tok/s` after TTFT. — refs: `scripts/regression-qwen-gemma.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `.tmp/qwen-gemma-regression/serve/qwen36-completions-stream.json`
- (2026-04-26) [SERVE] `bench:serve --report-json` preserves per-request rows and benchmark-observed server event timelines. Use `requests[]` for client-visible duration/TTFT/stream-cadence/usage and `serverRequests[]` for route-decision timing, prefill progress timing, first completion progress, completion/error timing, and silent event gaps. This is the evidence surface to inspect before claiming scheduler or long-prefill improvements. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/benchmark-serve-completions.ts`, `docs/reviews/2026-04-26-serve-benchmark-observability.md`
- (2026-04-26) [SERVE] Superseded by the Qwen/Gemma continuous tranches: real Qwen/Gemma serving regression should assert route honesty, not just endpoint liveness. When a family is still fallback-only, assert its fallback reason and zero batch counters; when it becomes scheduler-capable, assert `continuous:eligible`, scheduler phase counts, zero static batches, server-request coverage, and memory budgets. — refs: `packages/serve/scripts/regression-serve-matrix.ts`, `packages/serve/scripts/regression-serve-matrix.test.ts`, `docs/reviews/2026-04-26-qwen-continuous-serving-route.md`
- (2026-04-26) [SERVE] Model-lane queue timing is now part of serving evidence. `generation_model_lane_wait` distinguishes waiting for the local MLX lane from generation work, and `bench:serve --report-json` records `modelLaneWaitMs` in `serverRequests[]`; continuous scheduler internals still need separate scheduler-level queue/step telemetry. — refs: `packages/serve/src/transformers-engine.ts`, `packages/serve/scripts/benchmark-serve.ts`, `docs/reviews/2026-04-26-serve-model-lane-queue-telemetry.md`
- (2026-04-26) [SERVE] Continuous batching now has scheduler-owned phase telemetry through `generation_scheduler_phase` events. Use those events for queue/prefill/admission/first-token/finish/cancel timing instead of inferring scheduler behavior from generic event gaps or overloading `generation_batch_start`; Qwen/Gemma fallback rungs should continue asserting zero continuous scheduler phases until their cache semantics are implemented. — refs: `packages/transformers/src/infrastructure/generation/continuous-batch.ts`, `packages/serve/src/transformers-engine-continuous.ts`, `packages/serve/scripts/benchmark-serve.ts`, `docs/reviews/2026-04-26-serve-continuous-scheduler-telemetry.md`
- (2026-04-26) [SERVE] Streaming greedy requests can use the full-KV continuous scheduler for eligible LLaMA-like models; all SSE stream consumers must close model iterators on stop-sequence early exits so scheduler-backed streams abort rather than continuing hidden decode work. Qwen/Gemma remain single-lane fallbacks until their transformer cache semantics are proven below serving. — refs: `packages/serve/src/transformers-engine-continuous.ts`, `packages/serve/src/transformers-engine-streaming.ts`, `packages/serve/src/server-streaming.ts`, `packages/serve/src/server-responses-streaming.ts`, `docs/reviews/2026-04-26-serve-continuous-streaming.md`
- (2026-04-26) [GEMMA] Gemma static greedy batching needs a layer-pattern batch cache, not `BatchKVCache`: full-attention layers retain full KV history, sliding-attention layers retain trimmed windows, and Gemma 4 shared-KV layers must build masks from the active source key/value length. Prove transformer-level `generateBatchTokens()` parity before widening serving routes. — refs: `packages/transformers/src/infrastructure/cache/layer-pattern-batch.ts`, `packages/transformers/src/families/gemma3/model.test.ts`, `packages/transformers/src/families/gemma4/model.test.ts`, `docs/reviews/2026-04-26-gemma-layer-pattern-batch-cache.md`
- (2026-04-26) [SERVE] Gemma 3/4 greedy non-streaming serving can use static batching through the layer-pattern batch cache, including coalesced concurrent requests. Do not route Gemma 3/4 into the full-KV continuous scheduler until scheduler row extension/removal is layer-pattern-aware; streaming Gemma remains a single-route fallback. — refs: `packages/serve/src/transformers-engine.ts`, `packages/serve/src/transformers-engine-routing.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-26-gemma-static-serving-route.md`
- (2026-04-26) [SERVE] Interactive streaming should flush decoded text every token by default, with `streamDecodeInterval` / `--stream-decode-interval` as the explicit throughput-vs-responsiveness knob. Qwen-style streamed `</think>` separators should be trimmed before visible chat content, and final-flush stop detection must preserve `finish_reason: "stop"` instead of being overwritten by a later model `done` reason. — refs: `packages/serve/src/transformers-engine-streaming.ts`, `packages/serve/src/protocols/openai-chat-completion-streaming.ts`, `packages/serve/src/server-streaming.ts`, `docs/reviews/2026-04-26-serve-streaming-ux-and-stop-semantics.md`
- (2026-04-26) [GEMMA] Gemma 3/4 layer-pattern static batching should omit the left-padded attention mask for unpadded single-token decode (`queryLength === 1` and all effective left padding is zero). The mask is semantically unnecessary in that state, matches the single-request null-mask fast path, and avoids a real Gemma 4 static batch crash in MLX `arange` after prefill. — refs: `packages/transformers/src/infrastructure/masks.ts`, `packages/transformers/src/infrastructure/cache/layer-pattern-batch.ts`, `packages/transformers/src/families/gemma4/attention.ts`, `docs/reviews/2026-04-26-gemma-static-batch-mask-elision.md`
- (2026-04-26) [SERVE] Real serving regressions must budget streaming responsiveness, not just throughput and liveness. `bench:serve` reports `streamDecodeInterval`, and `regression:serve --real-models` now asserts TTFT/chunk-gap budgets for streaming rungs so buffered or coalesced output cannot pass unnoticed. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-26-gemma-static-batch-mask-elision.md`
- (2026-04-26) [SERVE] Full-KV continuous batching should treat long initial prompts as scheduler work, not an opaque first-admission wall. Long initial rows enter chunked prefilling, ready short rows are admitted before long rows continue chunking, and long prefilling rows rotate after each chunk to avoid FIFO head-of-line blocking. — refs: `packages/transformers/src/infrastructure/generation/continuous-batch.ts`, `packages/serve/src/transformers-engine.test.ts`, `docs/reviews/2026-04-26-continuous-initial-prefill-fairness.md`
- (2026-04-26) [QWEN] Superseded by the Qwen continuous route: Qwen 3.6 text greedy non-streaming serving first proved static batching through a Qwen-owned hybrid batch cache. Keep this as the static-cache provenance, but current greedy non-streaming Qwen serving should use continuous scheduling when the model exposes `createBatchCache()`. — refs: `packages/transformers/src/families/qwen3_5/batch-cache.ts`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `packages/core/src/fast.ts`, `packages/serve/src/transformers-engine-routing.ts`, `docs/reviews/2026-04-26-qwen-static-hybrid-batching.md`, `docs/reviews/2026-04-26-qwen-continuous-serving-route.md`
- (2026-04-26) [SERVE] Superseded by the streaming route tranche: Continuous scheduling became cache-generic over `TransformerBatchCache`, with static and continuous generation sharing `createBatchCacheForModel()`; this first enabled Gemma 3/4 greedy non-streaming routing, and later streaming routing once SSE evidence was added. — refs: `packages/transformers/src/infrastructure/generation/batch-cache-factory.ts`, `packages/transformers/src/infrastructure/generation/continuous-batch.ts`, `packages/serve/src/transformers-engine-routing.ts`, `docs/reviews/2026-04-26-cache-generic-continuous-batching.md`
- (2026-04-26) [QWEN] Superseded by the streaming route tranche for route status: `Qwen3_5TextBatchCache.extend()` is the cache prerequisite behind Qwen continuous serving. The extend path delegates full-attention KV to `BatchKVCache`, concatenates linear conv/recurrent state by batch axis only, and rejects asymmetric or padded linear state. This first enabled Qwen greedy non-streaming continuous serving; streaming was enabled only after separate SSE/cadence evidence. — refs: `packages/transformers/src/families/qwen3_5/batch-cache.ts`, `packages/transformers/src/families/qwen3_5/model.test.ts`, `packages/serve/src/transformers-engine-routing.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-26-qwen-continuous-serving-route.md`
- (2026-04-26) [QWEN] Qwen staggered continuous serving needs explicit endpoint coverage. A real delayed row exposed a valid one-token left-padded mask shape (`queryLength=1,totalKeyLength=133,pastLength=132`) that MLX `arange(start,start+1)` rejected on the generation stream; build one-token position ranges with `array([start])` and keep cache padding/offset semantics unchanged. The real serve matrix now includes a staggered Qwen rung expecting two continuous admissions, three admitted rows, nine scheduler phases, zero static batches, and batch size 2. — refs: `packages/transformers/src/infrastructure/masks.ts`, `packages/transformers/src/infrastructure/masks.test.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-26-qwen-staggered-continuous-mask.md`
- (2026-04-26) [SERVE] Qwen 3.6 text and Gemma 3/4 layer-pattern greedy streaming can route through the same cache-generic continuous scheduler as buffered generation once their batch caches are proven. Keep sampled/model-native-default requests single-route until batched sampling is implemented, and keep streaming route claims tied to SSE lifecycle/cancellation/cadence evidence in `bench:serve`/`regression:serve`. — refs: `packages/serve/src/transformers-engine-routing.ts`, `packages/serve/src/transformers-engine-continuous.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-26-qwen-gemma-continuous-streaming.md`
- (2026-04-26) [SERVE] Real Qwen/Gemma streaming batching evidence must assert per-request SSE health, not only aggregate chunk counts. The real serving matrix now includes concurrent `128x32@2` streaming rungs for Qwen 3.6 and Gemma 4 that require two route decisions, two server requests, every request to have TTFT/chunks/bytes, one continuous admission with two rows, seven scheduler phases, and max generation batch size two. — refs: `packages/serve/scripts/regression-serve-matrix.ts`, `.tmp/qwen-gemma-regression-stream-concurrent/qwen36-completions-stream-continuous.json`, `.tmp/qwen-gemma-regression-stream-concurrent/gemma4-completions-stream-continuous.json`
- (2026-04-26) [SERVE] Continuous batching now supports sampled/model-native-default requests by keeping `SamplerState` per scheduled row while still doing one batched model forward per decode step. Static batch generation remains greedy-only. Gemma 4 exposed that top-k static mask indices should be host-created constants rather than `arange()` inside or immediately after large sampled graphs. — refs: `packages/transformers/src/infrastructure/generation/continuous-batch.ts`, `packages/transformers/src/infrastructure/generation/continuous-batch-helpers.ts`, `packages/transformers/src/infrastructure/sampling/runtime.ts`, `docs/reviews/2026-04-26-sampled-continuous-batching.md`
- (2026-04-26) [DOCS] After Qwen/Gemma sampled continuous scheduling landed, planning docs needed a truth pass: current serving includes cache-generic continuous scheduling for eligible LLaMA-like, Qwen 3.6 text, and Gemma 3/4 layer-pattern requests, while prefix cache, paged KV, Anthropic, embeddings, production metrics, and advanced backend flags remain future work behind typed strategy seams. — refs: `docs/serving-runtime-strategy.md`, `PLAN.md`, `docs/runtime-optimization-matrix.md`, `docs/reviews/2026-04-26-serving-alignment-followup.md`
- (2026-04-27) [SERVE] Runtime strategy in `@mlxts/serve` is now a derived internal description of implemented behavior, not a new public flag surface. `/info`, route logs, and benchmark reports can say `scheduler=auto`, `cache=managed`, `attention=auto`, and `decoding=model`; future `paged`, `turboquant`, speculative, or custom-attention flags still require backend contracts, rejection paths, tests, and benchmark evidence before exposure. — refs: `packages/serve/src/serve-runtime-strategy.ts`, `packages/serve/src/server-info.ts`, `packages/serve/src/transformers-engine-routing.ts`, `docs/reviews/2026-04-27-serve-runtime-strategy-seam.md`
- (2026-04-27) [SERVE] Production metrics should be derived from the `ServeEvent` stream, not sprinkled beside execution call sites. `/metrics` now reports bounded Prometheus text metrics for HTTP requests, generation lifecycle, tokens, route decisions, model-lane waits, scheduler phases, batches, prefill chunks, and MLX memory; `/metrics` scrapes are excluded from HTTP counters, and known model lists collapse unknown model labels to `__unknown__`. — refs: `packages/serve/src/serve-metrics.ts`, `packages/serve/src/server.ts`, `packages/serve/src/model-server.ts`, `docs/reviews/2026-04-27-serve-metrics.md`
- (2026-04-27) [SERVE] Streaming observability belongs at the SSE writer seam, not in model generation or benchmark-only parsers. `generation_stream_*` events and `/metrics` now report server-side TTFT, terminal result, SSE/output frame counts, and byte counts; client-observed TTFT remains benchmark-owned. — refs: `packages/serve/src/server-stream-observability.ts`, `packages/serve/src/server-streaming.ts`, `packages/serve/src/server-responses-streaming.ts`, `packages/serve/src/serve-stream-metrics.ts`, `docs/reviews/2026-04-27-serve-stream-observability.md`
- (2026-04-27) [SERVE] Serving benchmark reports keep client and server streaming evidence separate: `requests[]` is client-observed SSE timing/cadence, while `serverRequests[].serverStream*` is server writer evidence from `generation_stream_*` events. Regression budgets should assert both for real streaming rungs without requiring server chunk counts to equal client chunk counts. — refs: `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-27-serve-benchmark-stream-evidence.md`
- (2026-04-27) [SERVE] Continuous scheduler admission needs a model-level budget, not only per-scheduler row caps, because one loaded model can create multiple scheduler keys for different sampling/default options. The first landed guard is a shared scheduled-token reservation budget derived from `maxTotalTokens * maxBatchSize`, with token-pressure telemetry in scheduler events, metrics, and benchmark reports. Budget wakeups must be release-driven, not "some capacity is free" snapshots, or partial-capacity deferrals can hot-loop. — refs: `packages/serve/src/continuous-scheduler-budget.ts`, `packages/transformers/src/infrastructure/generation/continuous-batch-admission.ts`, `docs/reviews/2026-04-27-continuous-scheduler-token-budget.md`
- (2026-04-27) [SERVE] Stream regression budgets must distinguish protocol lifecycle evidence from visible output evidence. Model-default sampled streams can complete with usage/finish and zero visible decoded chunks when generated special/EOS tokens decode to `""`; keep strict per-request output/TTFT assertions for deterministic visible-output rungs, and use lifecycle assertions for sampled token-array completions. — refs: `packages/serve/scripts/regression-serve-matrix.ts`, `packages/serve/scripts/regression-serve-matrix.test.ts`, `.tmp/qwen-gemma-regression-stream-lifecycle-rerun/serve/gemma4-completions-model-defaults-stream-continuous.json`
- (2026-04-27) [SERVE] Staggered serving evidence only proves fairness for heterogeneous load when the benchmark sends heterogeneous request shapes. Use `bench:serve --mixed-rungs 32768x128+128x32 --request-stagger-ms <n>` for long-prefill plus short-arrival proof; plain `--rungs ...@2` repeats the same shape for each request. — refs: `packages/serve/scripts/benchmark-serve-options.ts`, `packages/serve/scripts/benchmark-serve.ts`, `packages/serve/scripts/regression-serve-matrix.ts`, `.tmp/qwen36-mixed-harness-smoke.json`, `.tmp/gemma4-mixed-harness-smoke.json`
- (2026-04-27) [SERVE] Continuous scheduler budgets must preserve all configured pressures at once: prompt reservations, completion reservations, and the aggregate `maxTotalTokens * maxBatchSize` cap. Reporting split fields is useful, but never let split caps weaken the old total-token envelope; deferral reasons should identify prompt, completion, or aggregate scheduled-token pressure. — refs: `packages/serve/src/continuous-scheduler-budget.ts`, `packages/serve/src/serve-scheduler-metrics.ts`, `docs/reviews/2026-04-27-continuous-scheduler-split-budget.md`
- (2026-04-27) [SERVE] Higher-concurrency serving guardrails should include per-request fairness evidence, not only aggregate throughput. The Qwen/Gemma real matrix now covers `128x16@4` and `128x16@8` streaming continuous rungs with per-request TTFT budgets, server scheduler queued-time budgets, SSE lifecycle/output evidence, zero static/admission batches, split token pressure, and expected max generation batch size. — refs: `packages/serve/scripts/regression-serve-matrix.ts`, `docs/reviews/2026-04-27-qwen-gemma-high-concurrency-serving-regression.md`
