# Repo Memory

This file captures durable cross-session learnings for `mlxts` so future agent sessions do not need to rediscover the same sharp edges.

## How To Use This File

### Tier 1 — Must Read

- Read this section at the start of every session, immediately after `AGENTS.md`.
- Keep it short and durable. Promote only facts that should shape most future sessions.
- If Tier 1 grows, demote older or narrower items into Tier 2.

### Tier 2 — Lookup Log

- Do not read this section end to end by default.
- Search it when working on a related area.
- Append durable learnings here at the end of a session.

## Memory Update Rule

- Add durable learnings to Tier 2 using this template:
  - `(YYYY-MM-DD) [TAG] <learning> — refs: <file/doc/command>`
- Promote only cross-session critical items to Tier 1.
- Keep Tier 1 focused on durable repo facts and recurring sharp edges, not doctrine already stated in `AGENTS.md`.
- Archive obsolete items instead of silently deleting them.
- Do not put transient task notes or diary-style updates here.

## Tier 1 — Must Read

- `examples/nanogpt/` is the committed in-repo example and regression surface for the GPT proof path; prefer improving canonical `@mlxts/*` packages over deepening example-only abstractions, and document its commands from the example directory rather than as root scripts.
- When Nomi has authorized sub-agents, use them for non-trivial repo work. Complex changes in this repo benefit from a second opinion and deliberate orchestration across explorers/workers, especially when local context is already crowded.
- Heavy MLX commands are exclusive on one machine. Do not run benchmarks, soak runs, acceptance runs, or long training/proof commands in parallel.
- Runtime-sensitive production changes require a review artifact under `docs/reviews/`, plus `bun run check:runtime-review` before handoff.
- `bun run typecheck` and `bun run check:coverage` are required quality gates, not optional cleanup.
- Before writing a JS fallback for a GPU-facing operation, check whether `mlx-c` already exposes the op in `packages/core/native/build/_deps/mlx-c-src/mlx/c/ops.h`.
- For low-level backend questions, consult `.reference/mlx` and `.reference/mlx-c` alongside the vendored headers before inventing JS workarounds or custom native helpers.
- Keep runtime strategy out of public model identity and semantic names. Do not widen `CausalLM` or create duplicate model configs just to represent cache/backend/runtime variants.
- Before treating a performance gap as real, verify benchmark truth first. Paired reference runs must match decode shape and stopping behavior, and long-context or boundary-sensitive confirmation matters more than one flattering short run.

## Tier 2 — Lookup Log

- (2026-04-22) [PRODUCT] `examples/nanogpt/` is the committed nanoGPT example/regression surface; permanent contracts still belong in reusable package-owned surfaces instead of the example. — refs: `AGENTS.md`, `README.md`, `PLAN.md`
- (2026-04-22) [AGENTS] When Nomi authorizes sub-agents, treat them as standard force multipliers for non-trivial repo changes. Use them for second-opinion architecture checks, scoped implementation slices, and upstream truth-finding when local context is saturated; orchestration quality matters as much as using them at all. — refs: `AGENTS.md`, this session's Qwen3.5 / multimodal research thread
- (2026-04-22) [OPS] Heavy MLX commands are exclusive on a shared machine; do not run benchmark, soak, acceptance, memory, or long-run training commands in parallel. — refs: `AGENTS.md`
- (2026-04-22) [QUALITY] Runtime-sensitive diffs need a paired review artifact under `docs/reviews/`, and the artifact's `Files Reviewed` section must name the exact changed runtime-sensitive files. — refs: `AGENTS.md`, `docs/runtime-safety.md`
- (2026-04-22) [ARCH] Model contracts describe behavior, not runtime strategy; MoE stays a block-level swap inside `CausalLM`, and runtime/backend differences should not fork model identity. — refs: `AGENTS.md`, `docs/design-reasoning.md`
- (2026-04-22) [MLX-C] For GPU-facing functionality, check `mlx-c` first before introducing JS workarounds; fallback JS should stay limited to genuinely host-side work. — refs: `AGENTS.md`, `packages/core/native/build/_deps/mlx-c-src/mlx/c/ops.h`
- (2026-04-22) [REFERENCES] Keep first-party `.reference/mlx` and `.reference/mlx-c` available and up to date for low-level capability audits. Use them to decide whether to bind an existing MLX/MLX-C primitive, rely on compile, or introduce a custom native seam. — refs: `AGENTS.md`, `.reference/mlx`, `.reference/mlx-c`
- (2026-04-22) [MLX] First-party MLX already covers most low-level primitives we need before writing custom C helpers: convs, scatter/masked-scatter, slice updates, fast norm/rope/SDPA, compile, and custom kernel surfaces. The real gaps are narrower: hybrid cache policy, Qwen3.5-style recurrent linear attention, and selective `mlx-c` binding omissions such as the `slice_update_{add,prod,max,min}` family that exists in C++ but is not exposed in `mlx-c`. — refs: `.reference/mlx/mlx/ops.h`, `.reference/mlx-c/mlx/c/ops.h`, `.reference/mlx-c/mlx/c/fast.h`, `.reference/mlx-c/mlx/c/compile.h`
- (2026-04-22) [OPS] The canonical Phase 8 training proof currently lives as a manual self-hosted Apple Silicon workflow plus local `bun run examples/train-proof/index.ts`; do not describe it as an every-push CI gate until we intentionally promote it. — refs: `.github/workflows/training-proof.yml`, `examples/train-proof/README.md`, `docs/gates-and-milestones.md`
- (2026-04-22) [BENCHMARKS] Gemma 4 parity work showed that benchmark fairness can dominate the apparent gap: confirm EOS handling, decode length, and reference harness behavior before attributing a delta to model/runtime code. Paired truth comes before optimization heroics. — refs: `packages/transformers/scripts/benchmark-mlx-lm.py`, `packages/transformers/scripts/benchmark-common.ts`, `docs/reviews/2026-04-20-gemma4-parity-experiments.md`
- (2026-04-22) [FINE-TUNING] Narrow-task data teaches a model more effectively than broad assistant-style chat data. Small mixed SFT sets are fine for stack proof, but held-out behavior, not lower loss alone, decides whether a repo-specific LoRA actually got better. — refs: `examples/chat-canary/README.md`, `examples/chat-canary/review-judge/README.md`, `docs/reviews/2026-04-22-review-judge-lora-proof.md`
- (2026-04-22) [FINE-TUNING] Experimental proof assets should stay explicitly labeled as experimental. Keep promising LoRA datasets and canaries in the repo when they teach something real, but do not silently let them become a claimed product surface or finished benchmark. — refs: `examples/chat-canary/README.md`, `examples/chat-canary/review-judge/README.md`, `docs/reviews/2026-04-22-review-judge-lora-proof.md`
- (2026-04-22) [ALIGN] DPO proof reporting must use reference-aware reward metrics, not just policy-only chosen-vs-rejected log-prob wins. `raw_pref_acc` is a supplemental debug signal; the canonical readout is reward accuracy plus reward margin, with chosen/rejected rewards and log-probs recorded alongside loss. — refs: `packages/align/src/loss-utils.ts`, `examples/train-proof/runtime.ts`, `examples/train-proof/stages.ts`, `examples/train-proof/README.md`
- (2026-04-22) [ALIGN] When Phase 8 examples start duplicating dataset-level eval and fixed-step SFT/DPO loop glue, extract that recipe layer into `@mlxts/align` rather than growing a new framework package or deepening `examples/nanogpt/`. Keep CLI policy, proof presets, and teaching-specific flows in examples. — refs: `packages/align/src/recipes.ts`, `examples/lora-finetune/runtime.ts`, `examples/train-proof/runtime.ts`, `PLAN.md`
- (2026-04-22) [DOCS] Example-owned commands should be documented from the example directory (`cd examples/nanogpt && bun run ...`), not as root `package.json` scripts. — refs: `README.md`, `docs/setup.md`, `docs/product-surfaces.md`
- (2026-04-22) [ALIGN] Raw chat normalization belongs in `@mlxts/align` once multiple Phase 8 examples are reimplementing “build example, skip malformed rows, skip overlong rows, report kept/skipped counts.” Keep dataset-specific parsing and proof/example policy outside the package, but keep the normalization loop package-owned. — refs: `packages/align/src/chat-preparation.ts`, `examples/lora-finetune/data.ts`, `examples/train-proof/prepare.ts`
- (2026-04-22) [MULTIMODAL] Keep file and image decoding outside `@mlxts/transformers`; the package should own checkpoint-side preprocessing truth (`preprocessor_config.json`, prompt preparation, patchification), while examples or apps own local file I/O and resize/decode plumbing. — refs: `examples/qwen3_5-image/`, `packages/transformers/src/families/qwen3_5/preprocessing.ts`
- (2026-04-22) [MULTIMODAL] Qwen 3.5 / Qwen 3.6 wrapper prompts need batch-aware rope-delta reuse and prompt-side image placeholder expansion. Keep the repeated image-token span and per-batch position recovery inside the package-owned helper instead of scattering that logic across examples. — refs: `packages/transformers/src/families/qwen3_5/conditional.ts`, this session's Qwen multimodal tranche
- (2026-04-22) [OPS] Wrapper commands like `examples/nanogpt/src/run/acceptance.ts` and `examples/nanogpt/src/run/soak.ts` must not hold the shared runtime lock while launching the supervised run. The spawned supervisor owns the heavy MLX lock; wrapper-level post-run MLX work can reacquire it only after the supervised run exits. — refs: `examples/nanogpt/src/run/acceptance.ts`, `examples/nanogpt/src/run/soak.ts`, `examples/nanogpt/src/run/acceptance.test.ts`, `docs/reviews/2026-04-22-examples-nanogpt-migration.md`
- (2026-04-22) [GEMMA4] Dense-only fast paths must stay dense-only. If a family implementation reads raw `.weight` tensors for a compiled path, wrapped or quantized modules need a semantic fallback rather than being forced through the dense shortcut. — refs: `packages/transformers/src/families/gemma4/mlp.ts`, `packages/transformers/src/families/gemma4/mlp.test.ts`, `docs/reviews/2026-04-22-gemma4-mlp-lora-compatibility.md`
- (2026-04-22) [QWEN] Qwen 3.5 / Qwen 3.6 full-attention `q_proj` outputs are packed per head as `[query, gate]`, not as one global query half followed by one global gate half. When splitting packed projections, reshape to `[batch, seq, heads, 2 * headDim]` first, then split the last axis per head before flattening back. — refs: `packages/transformers/src/families/qwen3_5/attention.ts`, `packages/transformers/src/families/qwen3_5/attention.test.ts`, `.reference/mlx-lm/mlx_lm/models/qwen3_next.py`
- (2026-04-22) [TOKENIZERS] Hugging Face `tokenizer.json` added tokens can use sparse IDs above the base vocab size. `BPETokenizer` decode tables must register `added_tokens` by ID, not just base vocab entries, or real checkpoints like Qwen 3.6 will fail to decode tokens such as `<think>` / `</think>` even when generation is correct. — refs: `packages/tokenizers/src/bpe-base.ts`, `packages/tokenizers/src/bpe.test.ts`, `mlx-community/Qwen3.6-27B-4bit`
- (2026-04-23) [GENERATION] Long text streaming must not re-decode the full generated prefix on every token. That exact-but-host-heavy pattern turns large `maxTokens` runs into repeated O(n) decode work, severe slowdown, and avoidable crash pressure in chat surfaces. Batch stream flushes and do one final exact decode instead. — refs: `packages/transformers/src/generation.ts`, `packages/transformers/src/generation.test.ts`, `docs/reviews/2026-04-23-long-generation-chat-streaming.md`
- (2026-04-23) [GENERATION] `mlx-lm` uses one-token async lookahead during generation, so async lookahead is not a smell by itself. Keep cache views stable as values handed to attention, and treat huge long-context runs as a cache-policy problem requiring max-KV, quantized KV, paged cache, or memory-budget controls. — refs: `.reference/mlx-lm/mlx_lm/generate.py`, `.reference/mlx-lm/mlx_lm/models/cache.py`, `.reference/transformers/src/transformers/cache_utils.py`, `.reference/omlx/omlx/memory_monitor.py`
- (2026-04-23) [SERVE] `@mlxts/serve` should stay protocol-adapter-thin: OpenAI completions, chat completions, Responses/OpenResponses, and Anthropic messages normalize directly into one internal generation request, not into each other. Engines own generation; adapters own wire shape. — refs: `packages/serve/src/types.ts`, `packages/serve/src/protocols/openai-completions.ts`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [SERVE] Treat admission micro-batching and continuous token-level batching as different layers. `createMicroBatchingGenerationEngine()` is a correctness/test seam for coalescing nearby non-streaming requests into `generateBatch()`; production throughput still needs a scheduler/cache-aware engine behind the same contract. — refs: `packages/serve/src/batching-engine.ts`, `examples/serve-completions/index.ts`, `.reference/vllm-mlx/docs/guides/continuous-batching.md`, `.reference/omlx/omlx/engine_core.py`
- (2026-04-23) [SERVE] Real model serving should reject unsafe generation lengths at admission, expose `/v1/models`, and keep auth/model-id routing separate from the transformer engine. The first model-backed serving surface is intentionally non-streaming until the engine owns true async streaming/backpressure. — refs: `packages/serve/src/request-limits.ts`, `packages/serve/src/protocols/openai-models.ts`, `packages/serve/src/model-server.ts`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [SERVE] Real model serving is a first-class package API and CLI responsibility, not an `examples/` product surface. Examples can demonstrate flows, but operator-facing load/serve behavior belongs in `@mlxts/serve` and should be validated through package tests, CLI help, package builds, and root build. — refs: `packages/serve/src/model-server.ts`, `packages/serve/src/cli.ts`, `packages/serve/package.json`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [AGENT] Agent loops belong in `@mlxts/agent`; serving owns wire protocols and model execution, transformers own chat-template/tool prompt compilation, and agent owns model→tool→observation→continue orchestration. Avoid homemade completions prompt renderers when a package-owned chat endpoint and interaction profile exist. — refs: `packages/agent/src/loop.ts`, `packages/agent/src/chat-model.ts`, `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/transformers/src/chat-template.ts`
- (2026-04-23) [AGENT] Superseded: an earlier stabilizer made agent CLI turns default to deterministic generation after a sampled Qwen3.6 smoke hit native crash behavior. The product-standard fix is model-native defaults by omission, explicit `--greedy` / `--deterministic` for tool-loop safety, and targeted sampling containment/coverage instead of hiding model defaults. — refs: `packages/agent/src/cli.ts`, `packages/agent/src/chat-model.ts`, `packages/transformers/src/infrastructure/sampling/runtime.ts`, `docs/reviews/2026-04-23-serve-foundation.md`
- (2026-04-23) [SERVE] OpenAI-compatible adapters should preserve omitted sampling fields (`temperature`, `top_p`, `top_k`) so checkpoint `generation_config.json` applies. Qwen-style thinking controls belong in chat-template kwargs, and generated `<think>` text should surface as `reasoning_content`/`reasoningContent` rather than leaking into visible assistant content. — refs: `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/serve/src/protocols/openai-completions.ts`, `packages/agent/src/chat-model.ts`, `packages/transformers/src/chat-template.ts`
- (2026-04-23) [AGENT] Qwen3.6 may emit native tool calls as `<tool_call><function=list_files>...</function></tool_call>` even when the agent provides OpenAI tool schemas and a JSON envelope instruction. The agent parser must accept both the package JSON envelope and Qwen-style function blocks for local interactive use. — refs: `packages/agent/src/tool-calls.ts`, `packages/agent/src/tool-calls.test.ts`, live `qwen-smoke` PTY run
- (2026-04-23) [AGENT] Real interactive agent validation should cover multiple REPL turns, reasoning display, and at least one real tool observation continuing into a final answer. A silent max-iteration return is a usability bug; surface it as an explicit `[agent]` stop notice. — refs: `packages/agent/src/cli.ts`, `packages/agent/src/cli.test.ts`, live `qwen-smoke` PTY run
- (2026-04-23) [AGENT] `@mlxts/agent` can consume OpenAI chat-completion SSE through the optional `AgentModel.stream()` contract. The loop aggregates streamed content, reasoning, and `delta.tool_calls` before tool execution while emitting incremental `model_delta` events for CLI responsiveness; keep non-streaming `complete()` models as a compatible fallback. — refs: `packages/agent/src/types.ts`, `packages/agent/src/chat-model.ts`, `packages/agent/src/loop.ts`, `packages/agent/src/cli.ts`
- (2026-04-23) [EXAMPLES] Example-only runnable entrypoints do not all need declaration output. For `examples/nanogpt`, keep JS bundling for CLI/manager/acceptance/soak/supervisor/memory, but declaration build should cover exported package surfaces so repo-level helper imports like `scripts/runtime-command-lock.ts` do not leak into the example package type boundary. — refs: `examples/nanogpt/tsconfig.build.json`, `scripts/runtime-command-lock.ts`
- (2026-04-23) [QWEN] Qwen 3.6 decode memory failures came from multiple Qwen-only lifetime issues, not the server or async lookahead. The catastrophic leak was `gatedDeltaSequence()` retaining the final recurrent state without freeing its local handle (`~153 MB/token`). The remaining `~4 MB/token` slope was also wrong and came from hidden owned intermediates in the Qwen decoder block and gated-delta conv/projection path. The prompt 128 / generation 128 benchmark now measures flat active memory, while Llama, Gemma 3, and Gemma 4 controls stay flat. — refs: `packages/transformers/src/families/qwen3_5/block.ts`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `packages/transformers/src/families/qwen3_5/gated-delta.test.ts`, `scripts/runtime-sensitive-ops.ts`, `packages/transformers/scripts/benchmark-generation.ts`, `docs/reviews/2026-04-23-qwen3_6-decode-memory.md`
- (2026-04-23) [SERVE] OpenAI `stop` semantics must stay aligned across completions and chat, and SSE writers must flush buffered stop/reasoning tails even if a stream ends before an explicit final `done` event. Shared admission gates also need to release permits when `stream()` fails before yielding, or single-model serving can deadlock under `maxConcurrentRequests=1`. — refs: `packages/serve/src/protocols/openai-stop.ts`, `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/serve/src/server-streaming.ts`, `packages/serve/src/concurrency-engine.ts`, `docs/reviews/2026-04-23-serve-chat-streaming-and-admission.md`
- (2026-04-23) [SERVE] Bun SSE serving needs lifecycle-aware handling: disable Bun's per-request idle timeout for streaming responses, emit `request_complete` only after the stream actually finishes, and surface client disconnects as explicit cancellation instead of silent early socket loss. — refs: `packages/serve/src/server.ts`, `packages/serve/src/server-streaming.ts`, `docs/reviews/2026-04-23-serve-streaming-lifecycle.md`, `https://bun.sh/docs/runtime/http/server`
- (2026-04-23) [SERVE] True continuous batching is blocked below `@mlxts/serve` by the current single-sequence cache contract in `@mlxts/transformers`; admission micro-batching is not a substitute. The honest next throughput tranche is a batch-aware cache plus scheduler-owned decode loop, not more HTTP-layer wrappers. — refs: `packages/transformers/src/types.ts`, `packages/transformers/src/infrastructure/cache/index.ts`, `packages/serve/src/batching-engine.ts`, `.reference/mlx-lm/mlx_lm/models/cache.py`, `docs/reviews/2026-04-23-serve-streaming-lifecycle.md`
- (2026-04-23) [GENERATION] The first batch generation foundation is static, full-cache, non-streaming, and greedy-only: `BatchKVCache` tracks per-row offsets/left padding, LLaMA-like full-cache models can consume it, and `generateBatchTokens()` filters completed rows. Do not call this continuous batching; Qwen hybrid caches, Gemma layer-pattern/sliding caches, sampled batch decode, and serving scheduler integration remain separate tranches. — refs: `packages/transformers/src/infrastructure/cache/batch.ts`, `packages/transformers/src/infrastructure/generation/batch.ts`, `packages/transformers/src/families/llama-like/model.test.ts`, `docs/reviews/2026-04-23-static-batch-generation.md`
- (2026-04-23) [SERVE] The transformer-backed serve engine can now use `generateBatchTokens()` for eligible non-streaming greedy full-cache LLaMA-like groups. Keep Qwen, Gemma 3/4 layer-pattern caches, sampled/model-native-default requests, and streaming on the single-request path until a scheduler-owned engine handles those decode/cache shapes. — refs: `packages/serve/src/transformers-engine-generation.ts`, `packages/serve/src/model-server.test.ts`, `docs/reviews/2026-04-23-serve-static-batch-integration.md`
- (2026-04-23) [SERVE] `serveLoadedModels()` is the first-class programmatic multi-model surface for already-loaded checkpoints. Each model id gets its own transformer engine, request limits, concurrency gate, and micro-batch queue before the shared router dispatches requests, so multiple local models do not accidentally share cache/runtime state. — refs: `packages/serve/src/model-server.ts`, `packages/serve/README.md`, `docs/reviews/2026-04-23-serve-loaded-models.md`
- (2026-04-23) [GENERATION] Static greedy batch generation now accepts either scalar `maxTokens` or per-row `maxTokens` arrays, emits row-indexed token/done events, and skips zero-token rows before prefill. Serving uses that to batch eligible mixed-`max_tokens` requests while preserving original result order. — refs: `packages/transformers/src/infrastructure/generation/batch.ts`, `packages/serve/src/transformers-engine-generation.ts`, `docs/reviews/2026-04-23-static-batch-decode-v2.md`
- (2026-04-23) [SERVE] `serveModels()` is the first-class programmatic multi-source loader for serving several local or Hub checkpoints behind one endpoint. It loads sequentially to avoid RAM spikes, annotates progress with source/model context, owns the loaded models, and cleans up already-loaded models if a later load or server start fails. — refs: `packages/serve/src/model-sources.ts`, `packages/serve/src/model-sources.test.ts`, `docs/reviews/2026-04-23-serve-model-sources.md`
- (2026-04-23) [SERVE] The `mlxts-serve` CLI supports repeatable `--model <source>` / `--model <model-id=source>` entries for multi-model endpoints and routes them through `serveModels()`. Keep `--model-id` as a positional single-model shorthand only; reject ambiguous mixes instead of guessing. — refs: `packages/serve/src/cli-options.ts`, `packages/serve/src/cli.ts`, `docs/reviews/2026-04-23-serve-cli-multi-model.md`
- (2026-04-23) [SERVE] Non-streaming OpenAI chat completions format valid generated `<tool_call>...</tool_call>` envelopes as structured `message.tool_calls` with `finish_reason: "tool_calls"` when tools were enabled. The parser is local to `@mlxts/serve` and shared by the streaming tool-call tranche. — refs: `packages/serve/src/protocols/openai-chat-completions.ts`, `packages/serve/src/protocols/openai-chat-tool-calls.ts`, `docs/reviews/2026-04-23-serve-chat-tool-calls.md`
- (2026-04-23) [SERVE] Streaming OpenAI chat completions now buffer generated `<tool_call>...</tool_call>` envelopes and emit structured `delta.tool_calls` chunks with `finish_reason: "tool_calls"` when tools were enabled. Keep parsing conservative: malformed or incomplete tool-call-looking text remains visible content, and Qwen-style native function XML is parsed only after a complete envelope arrives. — refs: `packages/serve/src/protocols/openai-chat-tool-call-stream.ts`, `packages/serve/src/server-streaming.ts`, `docs/reviews/2026-04-23-serve-chat-tool-calls.md`
- (2026-04-23) [QWEN] Qwen 3.6 parity must use the fixed multi-trial harness before claiming speed deltas: capture `mlx-lm` before loading the Bun model, forward `prefill_step_size`, match warmup/trial count, and keep local-only profiling explicit with `--skip-mlx-lm-reference`. Compiling Qwen decay factors is a proven small win (`24.898 -> 25.945 tok/s` on prompt 128 / decode 128), but compiling the multi-output recurrent step through `compileMany()` crashed Bun and should not be retried casually; the next real seam is a narrow fused/native gated-delta helper with the TS recurrence as fallback/oracle. — refs: `packages/transformers/scripts/benchmark-generation-parity.ts`, `packages/transformers/scripts/benchmark-mlx-lm.py`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [QWEN] The Qwen 3.6 parity keeper stack is: native Metal gated-delta helper, mixed-dtype q/k/v/g/beta inputs with fp32 recurrent state, contiguous conv-cache tail, causal SDPA marker for non-window cached prefill, one hoisted full-attention mask per model forward, and eval-only fused quantized `b/a` projections. This moved staged rungs to practical parity: `1024/128` slightly ahead of `mlx-lm`, `10000/128` about `0.7%` under, `1024/1024` about `0.3%` under, and local long outputs `128/10000` plus `128/20000` completed at `0.07 MB/token` active slope. — refs: `packages/core/native/mlxts_core_ops.cpp`, `packages/transformers/src/families/qwen3_5/gated-delta.ts`, `packages/transformers/src/families/qwen3_5/model.ts`, `packages/transformers/src/infrastructure/masks.ts`, `packages/nn/src/quantized-linear.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [BENCHMARKS] For Qwen 3.6 capability claims, use the existing suite as a staged matrix instead of inventing a new harness: paired `bench:generation:parity` for `128/128`, `1k/128`, `5k/128`, `10k/128`, and output rungs like `1k/512` and `1k/1024`; local `bench:generation:parity --skip-mlx-lm-reference` for extreme outputs such as `10k` and `20k`; and `bench:generation:context` for retrieval/context ladders. Long-context config must read nested `text_config.max_position_embeddings`; Qwen 3.6 advertises `262144`, but local `128k` already peaked at `42.550 GB`, so 262k should wait for serving admission/memory preflight. — refs: `packages/transformers/scripts/benchmark-generation-parity.ts`, `packages/transformers/scripts/benchmark-long-context.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [LONG-CONTEXT] Qwen long-context retrieval benchmarks should disable thinking at prompt compilation and grade the first non-empty generated answer line for fixed-length decode, while still printing the full response. Qwen3.6 retrieved markers exactly at `32k`, `64k`, and `128k` with `0.00 MB/token` active decode slope; measured peaks were `25.995 GB`, `31.410 GB`, and `42.550 GB` respectively. — refs: `packages/transformers/scripts/benchmark-long-context.ts`, `packages/transformers/scripts/benchmark-long-context.test.ts`, `docs/reviews/2026-04-23-qwen3_6-parity-experiments.md`
- (2026-04-24) [SERVE] After Qwen's long-context/long-output proof, the next reliability gap is serving orchestration rather than another decode micro-op: add long-context admission/memory preflight, prefill progress logs, cancellation, and Qwen-aware scheduler/cache support before claiming high-concurrency local serving. `mlx-lm` has batch generator patterns, `vllm-mlx` has scheduler/prefix-cache structure, `omlx` has memory enforcement/pinning, and `rapid-mlx` has chunked prefill/decode interleaving and streaming post-processing worth studying. — refs: `packages/serve/src/batching-engine.ts`, `packages/serve/src/transformers-engine-generation.ts`, `.reference/mlx-lm/mlx_lm/generate.py`, `.reference/vllm-mlx/vllm_mlx/scheduler.py`, `.reference/omlx/omlx/process_memory_enforcer.py`, `.reference/rapid-mlx/vllm_mlx/scheduler.py`
